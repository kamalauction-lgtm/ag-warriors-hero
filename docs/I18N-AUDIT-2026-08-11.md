# I18N Audit — 2026-08-11 (Kamal: "full audit bahasa MY/EN/ID")

Rule (2026-08-11): MY = EN/BM, EN default · ID = ID/EN, ID default. Language must cover SYSTEM + CONTENT.

## Mechanisms in the codebase (4)
1. `t('key')` — lib/i18n.ts STRINGS (en/id/bm — 193 keys, all three IDENTICAL key sets ✓, dictionary itself is clean)
2. inline `L(en, ms, id)` — modules/learn/LessonEngine `useL`
3. `useTLocale()` — trilingual jsonb DB content
4. private dict in modules/talent/* (en / ms-MY / id-ID)

## FIX ORDER (agent-facing impact on Indonesian team)
1. **Income.tsx `PrimaryID`** (~120 strings, the ID-only calculator is 100% English!) + **TimeBox** (~35) + **HelpRequest** (~15) + **CoachBrief** (~10) — all render on My Day / money screen daily.
2. **Grow.tsx / Team.tsx / MyDay.tsx residuals** (~60 short strings, cheap wins).
3. **Career.tsx content** — chrome translated, but COMMON rules + rank `focus` strings English; `career_ladder.focus` is plain text → needs trilingual jsonb migration for admin-editable content.
4. **WhatsApp opener hardcoded MALAY** in Caller.tsx:491 + Leads.tsx:416 (`saya dari IQI berkenaan {project} 😊`) — Indonesian agents send Malay to Indonesian PROSPECTS (customer-visible!). + Onboarding.tsx (mixed Malay/English to everyone).
5. **modules/coach/*** (ReviewQueue ~45, CoachBoard ~20, FocusManager, HelpInbox) — English-only but granted to role 'leader' → Indonesian team leaders review evidence in English. Decide tier.
6. **Privacy.tsx / ResetPassword.tsx** — EN+BM only, NO Indonesian at all (Privacy is Play-Store-facing).

## Wrong-language-to-everyone (worst bugs)
- Leads.tsx FIELD_LABELS hardcoded Indonesian ("Pemicu beli", "Budget/bln") shown to MY (demo path; mirrored Admin.tsx:1891)
- Onboarding.tsx "Selamat datang!", "✓ Saya faham" (Malay to ID users)
- HelpRequest "Minta Bantuan" title for everyone
- TimeBox "Belum — not done" mix
- Poster captions: MY gets EN, never BM; ID gets BI ✓
- Elite.tsx fully English (~95) — army theme, Kamal may accept EN tier or not
- ResetPassword errors EN·BM, ID omitted

## Entirely untranslated (zero t()/L())
Income, Elite, Directory, Join, Onboarding, Poster, Privacy, ResetPassword, TimeBox, HelpRequest, WarriorProfile, ComingSoon, charts, Badges, all modules/coach/*, challenge/Coaches+Reports, Admin.tsx (~400+) and every *Admin/CallerSetup/CallerImport (admin tier — English acceptable per current stance).

## Clean already
Caller.tsx (L×87 — only the WA template gap), Atlas, BoothDuty, LessonEngine, GrowOnboarding, Social, Academy (near-full; 12 ROLE_LABELS still EN), talent modules, Login, Register, Sales (near-full), Pipeline, Challenge/Journey, Notifications (near-full).

## Content (DB) gaps
- career_ladder.focus / group_note: plain text, not trilingual
- notifications title/body: single-language at insert time
- Directory categories single-language
